#include "main.h"

/**
 * add - Function that adds two integers.
 * @a: One of the two integers to be added
 * @b: One of the two integers to be added
 *
 * Return: Returns results of the addition of integers.
 */
int add(int a, int b)
{
		int result;

		result = a + b;
		return (result);
}
